[tox]
envlist = py37

[testenv]
extras = test
passenv = CI TRAVIS TRAVIS_* CODECOV_* TOXENV DISPLAY CODACY_*
deps =
    pytest-xvfb
    codecov>=1.4.0
conda_deps=
    yarn
    openbabel
    rdkit
    c-compiler
    cxx-compiler
conda_channels=
    conda-forge
commands =
    pytest --pyargs reacnetgenerator --cov {envsitepackagesdir}/reacnetgenerator -s
    - codecov -e TOXENV
